# Database credentials for KorbanMBG Python scripts & Go API.
# Copy to api/.env on the server (and project root for local runs), then fill in.
# Loaded by cron via: set -a; . api/.env; set +a
PORT=8090
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=
DB_NAME=korbanmbg
